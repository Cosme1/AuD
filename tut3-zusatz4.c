#include <stdio.h>

int main() {
    int matches, turn;
    printf("Select the number of matches you want to pick: \n");
    scanf("%d", &matches);
    turn = 1;
    while (A){
        if (turn == B){
            // Zug des Computers
            C
            // Bekanntgabe der verbleibenden Hoelzer
            D
        }
        else {
            // Zug des Menschen
            E
        }
    }
    F
    return 0;
}